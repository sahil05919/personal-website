// Shared component exports only.

/**
 * Delete these files:
 *   Hero.tsx  Genesis.tsx  Principles.tsx  Interests.tsx
 *   Moments.tsx  Detours.tsx  Snapshot.tsx  JourneyCTA.tsx
 *   Portrait.tsx  Colophon.tsx  ChromeUnwind.tsx
 *
 * ChromeUnwind is dropped deliberately. It existed to strip the navbar over a
 * silent opening; the opening is no longer silent, and fading the chrome over
 * a display-scale hero would fight the hero rather than frame it.
 *
 * Mission.tsx — STILL OPEN. The children line ("they'll discover more than my
 * career") has no home. It's about the website and this page is about him.
 * Don't delete until it lands somewhere.
 */
